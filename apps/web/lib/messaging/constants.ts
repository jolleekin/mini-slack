export const MAX_WORKSPACE_NAME_LENGTH = 50;

export const MAX_WORKSPACE_SLUG_LENGTH = 24;

export const MAX_CHANNEL_NAME_LENGTH = 50;

export const SLUG_REGEX = /^[a-z]([a-z0-9-]*[a-z0-9])?$/;
