# Project Structure

## Monorepo Layout

```
mini-slack/
├── apps/
│   └── web/                  # Next.js app (main web client + API server)
├── packages/
│   ├── db/                   # Drizzle schema + migrations (shared across apps)
│   ├── errors/               # Typed AppError classes (NotFoundError, ForbiddenError, etc.)
│   ├── events/               # Domain event type definitions (Transactional Outbox payloads)
│   ├── i18n/                 # Error message translation utility
│   ├── id-gen/               # ID generation (sequential + random)
│   ├── logger/               # Pino-based structured logger
│   └── observability/        # OpenTelemetry setup
├── services/
│   └── wss/                  # Isolated WebSocket service (real-time broadcast)
├── infra/
│   └── docker-compose.yml    # Local Postgres + Redis
├── docs/                     # Architecture docs, PRD, scaling strategy
├── .env                      # Root env file (injected into all workspaces via dotenv-cli)
├── tsconfig.base.json        # Shared TS config extended by all packages/apps
└── package.json              # Root workspace definition
```

## Web App (`apps/web/`)

```
apps/web/
├── app/
│   ├── (landing)/            # Public landing page (unauthenticated)
│   ├── (auth)/               # Auth pages: /signin, /welcome
│   │   └── layout.tsx        # Auth shell layout
│   ├── (app)/                # Protected app pages (requires JWT)
│   │   ├── layout.tsx        # App shell layout
│   │   └── workspaces/       # Workspace list page
│   └── api/
│       ├── auth/[...all]/    # Better Auth handler
│       └── rpc/[[...route]]/ # oRPC handler (all domain procedures)
├── lib/
│   ├── context.ts            # ServiceContext / WorkspaceServiceContext types
│   ├── db.ts                 # Drizzle db singleton + Db/Tx type aliases
│   ├── constants.ts          # Shared constants (e.g. WORKSPACE_ID_HEADER)
│   ├── server-utils.ts       # publishEvent(), mapUniqueViolationToConflict()
│   ├── identity/
│   │   ├── auth.ts           # Better Auth server instance
│   │   └── auth-client.ts    # Better Auth browser client
│   ├── messaging/            # Domain service layer (business logic)
│   │   ├── workspaces/       # service.ts, types.ts
│   │   ├── channels/         # service.ts, types.ts
│   │   ├── messages/         # service.ts, types.ts, system.ts, utils.ts
│   │   └── workspaces/
│   │       ├── members/      # service.ts, types.ts
│   │       └── invitations/  # service.ts, types.ts, constants.ts
│   └── rpc/
│       ├── client.ts         # oRPC browser client factory
│       ├── get-workspace-member-role.ts
│       └── routers/          # One router file per domain; composed in index.ts
│           ├── orpc.ts       # Base procedure + workspaceProcedure middleware
│           ├── workspaces.ts
│           ├── channels.ts
│           ├── messages.ts
│           ├── workspace-members.ts
│           ├── channel-members.ts
│           ├── invitations.ts
│           └── index.ts      # Composes all routers → RPCRouter type
├── tests/                    # Mirrors lib/ structure
│   ├── helpers/
│   │   ├── db.ts             # createTestDb() — PGlite in-memory DB with migrations
│   │   ├── router-test.ts    # getRpcClient(), mockSession() for router tests
│   │   └── server-only-mock.ts
│   ├── workspaces/           # service.test.ts, router.test.ts
│   ├── channels/
│   ├── messages/
│   ├── workspace-members/
│   ├── channel-members/
│   ├── invitations/
│   ├── identity/
│   └── proxy/
└── proxy.ts                  # Next.js middleware (JWT verification + route protection)
```

## DB Package (`packages/db/`)

```
packages/db/
├── schema/
│   ├── identity/             # users, accounts, sessions, verifications
│   ├── messaging/            # workspaces, channels, messages, files, enums
│   ├── infrastructure.ts     # outbox, id_sequences tables
│   ├── audit.ts              # Shared audit fields (createdAt, updatedAt)
│   ├── types.ts              # Shared column type helpers (idType, etc.)
│   └── index.ts              # Re-exports all schema
├── migrations/               # SQL migration files (generated by Drizzle Kit)
└── drizzle.config.ts
```

## Key Conventions

### Service Layer Pattern
Each domain has a `service.ts` (pure functions) and `types.ts` (Zod schemas + TS types). Services accept a `ServiceContext` or `WorkspaceServiceContext` as the first argument — never import `db` directly.

### Context Hierarchy
- `ServiceContext` — base; `workspaceId` is optional
- `WorkspaceServiceContext` — `workspaceId` is guaranteed (set by `workspaceProcedure` middleware)
- Use `requireUser(ctx)` to narrow `actor` from `Actor` to `UserActor`

### RPC Router Pattern
Routers in `lib/rpc/routers/` are thin — they only wire input schemas to service functions. Business logic lives exclusively in `lib/messaging/`.

### Transactional Outbox
All mutations that produce domain events must call `publishEvent()` inside the same `db.transaction()` block. Never publish events outside a transaction.

### Input Validation
All RPC inputs are Zod schemas defined in the domain's `types.ts` and passed to `.input()` on the procedure. No raw unvalidated input reaches service functions.

### Testing

**Co-located tests** (live next to the source file):
- **Unit tests** — `component.test.tsx` beside `component.tsx`
- **Property-based tests** — `component.property.test.tsx` beside `component.tsx`
- Use these for pure functions and UI components that have no dependency on shared test infrastructure

**`tests/` directory** (mirrors `lib/` structure, e.g. `lib/messaging/workspaces/service.ts` → `tests/workspaces/service.test.ts`):
- **Service tests** — use `createTestDb()` (PGlite) to test real SQL, no mocks
- **Router tests** — use `getRpcClient()` + `mockSession()` to test the full HTTP handler stack
- **Cross-cutting tests** — concerns with no single source module to co-locate with (e.g. `tests/proxy/`, `tests/identity/`)
- Use these whenever the test depends on shared helpers in `tests/helpers/`

**`tests/helpers/`**:
- `db.ts` — `createTestDb()` (PGlite in-memory DB with migrations)
- `router-test.ts` — `getRpcClient()`, `mockSession()` for router tests
- `server-only-mock.ts` — mock for `server-only` imports

### Package Imports
Internal packages use the `@mini-slack/*` scope (e.g. `@mini-slack/db`, `@mini-slack/errors`). Within `apps/web`, use the `@/` alias for local imports.
